import 'package:flutter/material.dart';

class BigAdd extends StatefulWidget {
  const BigAdd({super.key});

  @override
  State<BigAdd> createState() => _BigAddState();
}

class _BigAddState extends State<BigAdd> {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("bigadd"),
    );
  }
}